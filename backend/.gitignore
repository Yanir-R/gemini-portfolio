# Python virtual environment
venv/
.venv/
.venv*/
env/
ENV/

# Python cache files
__pycache__/
*.py[cod]
*$py.class

# Environment variables
.env
.env.*
.env.production
.env.*.production 

# IDE specific files
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Python distribution/packaging
dist/
build/
*.egg-info/
*.egg

# Local development settings
instance/
.webassets-cache

# Logs
*.log
logs/

# Test coverage
htmlcov/
.tox/
.coverage
.coverage.*
.cache
coverage.xml
*.cover
# Chat evaluation harness. Deliberately unpublished: the golden questions encode
# the ways this chat can be made to misbehave (injection probes, extraction
# attempts, the claims it must refuse to invent), which is operational knowledge
# rather than something the site benefits from shipping. The durable copy lives
# in the private vault - see backend/README.md.
evals/

# Runtime record of addresses submitted through the contact flow. Written by
# log_collected_email() into the working directory, so it appears in the repo
# root on any local run. It contains third-party personal data and this
# repository is public.
collected_emails.json
