# Cloudflare Pages: serve index.html for client-side routes so deep links like
# /projects/reelsensei and /about resolve instead of 404ing. Mirrors the
# `try_files $uri $uri/ /index.html` rule the nginx container used.
/*    /index.html   200
